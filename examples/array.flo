colors: str[] = ["red", "beige", "green", "blue", "yellow"]
colors[0] = "violet"
numbers: num [] = [1, 2, 4, 5, 6]
++numbers[1]
println(colors)
// I probably need a seperate append method
colors - "grey"
//modifiedColors[2]++
// Another bug to fix
println(numbers as str [])
colors[3]+="-green"
println(colors)
println("grey" in colors)
println(numbers)