// This example converts an input to binary
base = 2
print("Enter a number to change to base: ")
number = input()
result = ""
while number > 0 {
    mod = (number % base)
    result = mod + result
    number = (number - mod) / base
}
println(result)

