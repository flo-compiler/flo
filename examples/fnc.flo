fnc map(array: int [], func: (int) => int): int [] {
    arr = [func(array[0])]
    for i = 1; i < len(array); i++{
        arr << func(array[i])
    }
    return arr
}
original =  [1, 2, 3, 4, 5]
doubled = map(original, (x: int)=>x*2)
println(doubled)
println(original)