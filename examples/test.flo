fnc main(): uint {
    let str = input()
    let name = "aba"
    let found = -1
    while (found = str.find(name, found + 1 as uint)) != -1 {
        println("Found '$name' at $found")
    }
    return 0
}