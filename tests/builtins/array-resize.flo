class NO {}
fnc resize_array(arr: NO[], n: uint){
    for i in arr.length..n {
        arr << null
    }
}
fnc main(): int {
    let arr: NO[] = []
    for elem in 0..64 {
        let i = elem * 2 as uint 
        resize_array(arr, i)
        arr[i] = null
    } 
    return 0
}