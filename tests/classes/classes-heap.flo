class Vector {
    x: int
    y: int
}
fnc write(fd: int, str: i8*, len: int)
fnc unit_vector(): Vector {
    let vec: Vector = new Vector()
    vec.x = 1
    vec.y = 2
    return vec
}
fnc main(): int {
    let vec = unit_vector()
    ++vec.y
    return vec.y
}