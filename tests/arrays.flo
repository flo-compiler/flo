fnc main() {
    colors: str[] = ["red", "beige", "green", "blue", "yellow"]
    colors[0] = colors[1]
    numbers: int [] = [1, 2, 4, 5, 6]
    multi_array: float [][]  = [[1.0, 2.2, 4.0], [3.3, 2.4, 3.5]]
    ++numbers[1]
    println(multi_array)
    println(numbers == [1, 3, 4, 5, 6])
    println(colors[0])
    colors << "grey"
    println(len(colors))
    colors[3]+="-green"
    println(colors)
    // println(numbers as str [])
    // println("grey" in colors)
    println(numbers)
}